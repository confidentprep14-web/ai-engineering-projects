boto3==1.35.93
sagemaker==2.236.0
xgboost==2.1.3
mlflow==2.19.0
pandas==2.2.3
# NOTE: spec pins numpy==2.2.1, but sagemaker==2.236.0 requires numpy<2.0
# (pip's resolver reports ResolutionImpossible otherwise). Left unpinned so
# pip picks a numpy version compatible with sagemaker; same approach already
# used in batch-inference/requirements.txt, model-serving/requirements.txt,
# ab-testing/requirements.txt, and model-monitoring/requirements.txt, which
# also pin both boto3==1.35.93 and sagemaker==2.236.0 and likewise omit a
# numpy pin.
numpy
python-dotenv==1.0.1
pytest==8.3.4
