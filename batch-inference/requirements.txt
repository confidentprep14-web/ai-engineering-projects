boto3==1.35.93
sagemaker==2.236.0
# NOTE: spec pins xgboost==2.1.3, but that version's XGBClassifier.load_model()
# crashes under scikit-learn==1.6.0 (AttributeError: 'super' object has no
# attribute '__sklearn_tags__' inside xgboost/sklearn.py's is_classifier()
# check) -- a real upstream incompatibility, reproduced independent of MLflow.
# xgboost==2.1.4 (the next patch release) fixes it while staying on the same
# minor line. Same fix applied in experiment-tracking/requirements.txt; this
# project loads a model that was saved by that version, so the pin must match.
xgboost==2.1.4
mlflow==2.19.0
pandas==2.2.3
# NOTE: spec pins numpy==2.2.1, but sagemaker==2.236.0 requires numpy<2.0
# (pip's resolver reports ResolutionImpossible otherwise). Left unpinned so
# pip picks a numpy version compatible with sagemaker; same approach already
# used in local-to-sagemaker/requirements.txt, which also pins both
# boto3==1.35.93 and sagemaker==2.236.0 and likewise omits a numpy pin.
numpy
scikit-learn==1.6.0
python-dotenv==1.0.1
pytest==8.3.4
